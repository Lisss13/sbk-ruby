/* Scroll Section */
  
  (function($){
      $(window).on("load",function(){
        
        $(".sidebar-nav").mCustomScrollbar({
          autoHideScrollbar:true,
          theme:"sm-line"
        });
        
      });
    })(jQuery);
